export class RequestHandler {
	constructor(request, middlewareManager, methodManager) {
		this.request = request;
		this.middlewareManager = middlewareManager;
		this.methodManager = methodManager;
	}

	async run() {
		// TODO
	}
}
